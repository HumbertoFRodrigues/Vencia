Square icon-only control for row actions and toolbar affordances.

```jsx
<IconButton icon="ellipsis" label="Mais acções" />
<IconButton icon="pencil" label="Editar" variant="secondary" />
```

Always pass `label`. Use `ghost` inside table rows, `secondary` when it sits alone next to inputs.
