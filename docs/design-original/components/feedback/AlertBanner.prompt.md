Inline notice at the top of a page or panel — expired access, overdue payment, suspension record.

```jsx
<AlertBanner tone="danger" title="Este acesso terminou" action={<Button variant="danger" size="sm">Suspender</Button>}>
  É necessário renovar ou suspender o serviço.
</AlertBanner>
```
