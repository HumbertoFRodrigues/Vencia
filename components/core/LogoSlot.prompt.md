The "Adicionar logo personalizado" upload target — service library entries, new-subscription forms, company settings.

```jsx
<LogoSlot />
<LogoSlot src="/assets/logos/mpesa.png" label="Logo do serviço" hint="Substituir imagem" />
```

The image is always contained (never cropped) inside a fixed square so every service keeps a consistent presentation.
