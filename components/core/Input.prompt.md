Labelled text field. Use `suffix` for currency and day units rather than putting them in the label.

```jsx
<Input label="Valor" placeholder="1500" suffix="MZN" />
<Input label="Email" icon="mail" defaultValue="joao@email.com" />
<Input label="Telefone" error="Número inválido" />
```
