Left navigation with the product's eight destinations. Import `NAV_ITEMS` to get the canonical order and icons.

```jsx
<SidebarNav active="vencimentos" onSelect={setRoute} />
```

The active item is a white pill on the grey app background — no coloured fill, no left border accent.
